/// Shared user profile model used across the app.
class KokoProfile {
  final String name;
  final String avatarUrl;

  const KokoProfile({
    required this.name,
    required this.avatarUrl,
  });
}
